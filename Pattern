#include<stdio.h>
void main()
{
	int i,n,a,sum,num;
	a=1;
	sum=0;
	printf("Enter the limit: ");
	scanf("%d",&n);
	for(i=1;i<n;++i)
	{
		a=a*2;
		num=a-1;
		printf("%d  ",num);
		sum=sum+num;
	}
	printf("\nSum : %d",sum);
}
